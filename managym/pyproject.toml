[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "managym"
version = "0.2.0"
requires-python = ">=3.8"

[tool.maturin]
features = ["python"]
module-name = "managym._managym"
manifest-path = "Cargo.toml"
