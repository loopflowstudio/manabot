# Open questions / assumptions

- `SpellCountered` event type exists in the stable schema but is not emitted by current gameplay paths (no dedicated counterspell resolution yet). It will be emitted once Counterspell gets a proper resolution path — likely before or alongside sprint 04.
