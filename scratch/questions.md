# Open questions / assumptions

- Assumption: `cargo test -p managym` from repo root is not runnable because the repo root has no Cargo workspace manifest. Verification used `cd managym && cargo test -q`.
